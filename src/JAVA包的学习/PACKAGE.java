package JAVA包的学习;
/* 1.package:
 * A.作用：避免类的命名重名。
 * B.包名可以有层次结构，同包之中的类不能够同名
 * C.包之中的包使用.来进行分隔的操作的，就像计算机之中的文件目录一样。
 * D.类的全称：包名，类名，建议包名所有字母都小写
 *
 * 2.import:
 * A.同包之中的类可以直接进行访问
 * B.不同包之中的类不能够直接进行访问，要想访问只能使用如下方式
 * C.先import声明类再使用类-----建议
 * D.类的全称------------------太繁琐了，不建议这样去进行使用，
 * 当本包之中的类和引用同名时，只能够使用类的全称。
 */
public class PACKAGE {
}
